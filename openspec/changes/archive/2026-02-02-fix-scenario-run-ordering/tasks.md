## 1. Fix Scenario Data
- [x] 1.1 Update `scenarios.ts` to reverse the generated card arrays in sequence creation helpers so they are Ascending (Low to High).

## 2. Update UI
- [x] 2.1 Update `Run.tsx` to reverse the `data` (cards) array before passing it to `RunDisplay`.
    - Ensure this creates the desired Descending (High to Low) visual order.
